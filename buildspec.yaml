version: 0.1
phases:
  install:
    commands:
      - npm install request
      - aws cloudformation package --template-file example.yaml --s3-bucket haolinj-functions-bucket 
                                   --output-template-file serverless-output.yaml
artifacts:
  type: zip
  files:
    - serverless-output.yaml