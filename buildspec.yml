version: 0.1
phases:
  install:
    commands:
      - npm install request
      - aws cloudformation package --template-file example.yml --s3-bucket haolinj-functions-bucket 
                                   --output-template-file serverless-output.yml
artifacts:
  type: zip
  files:
    - serverless-output.yml